# Production environment configuration
# Database configuration (to be set in production environment)
DATABASE_URL=

# Mail configuration (to be set in production environment)
MAIL_HOST=
MAIL_PORT=
MAIL_SECURE=
MAIL_USER=
MAIL_PASS=
MAIL_FROM=

# Mail forwarding (optional)
MAIL_FORWARD_TO=

# JWT configuration (to be set in production environment)
JWT_SECRET=
JWT_REFRESH_SECRET=
JWT_EXPIRES_IN=

# Application configuration
NODE_ENV=production

# Frontend URL (to be set in production environment)
FRONTEND_URL=
